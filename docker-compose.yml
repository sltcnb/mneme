services:
  mneme:
    build: .
    image: ghcr.io/sltcnb/madeleine:latest
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - MNEME_DATA=/data
    command: ["serve", "--host", "0.0.0.0", "--port", "8080"]
